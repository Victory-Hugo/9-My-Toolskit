# 要求

写一个`/mnt/f/onedrive/文档（科研）/脚本/Download/9-My-Toolskit/1-下载数据/1-NCBI/pipe/4-xml→tsv.sh`脚本，功能如下：

- 输入：一个 包括了大量的  XML 文件的路径
- 输出：一个 TSV 文件，包含了 XML 文件中提取的**所有字段**信息，每行对应一个 XML 文件。

若使用python，需要使用双模块。
使用生物信息学pipeline的skill。

注意，tsv文件必须包括所有字段信息，相同的字段为1列。