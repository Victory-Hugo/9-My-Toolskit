
var doc = app.activeDocument;
var textFrames = doc.textFrames;
var targetText;
var changed = 0;

targetText = "Gelao_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Blang_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Mulam_Guangxi";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Bai_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Wa_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Chuanqing_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Pumi_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Raojia_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Lahu_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Dong_Guangxi";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Mongolian_Xinjiang";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Kazakh_Xinjiang";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Gelao_Hunan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Xijia_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Dongjia_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Gejia_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Han_Yunnan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Qiang_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Maonan_Guangxi";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Caijia_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Gaoshan_Fujian";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Han_Taiwan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Zhuang_Guizhou";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Dai_Guangdong";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

targetText = "Zhuang_Taiwan";
for (var i = 0; i < textFrames.length; i++) {
    if (textFrames[i].contents == targetText) {
        textFrames[i].textRange.characterAttributes.fillColor = new RGBColor();
        textFrames[i].textRange.characterAttributes.fillColor.red = 255;
        textFrames[i].textRange.characterAttributes.fillColor.green = 0;
        textFrames[i].textRange.characterAttributes.fillColor.blue = 0;
        changed++;
    }
}

alert(changed + ' text items changed to red.');
